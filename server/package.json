{
  "name": "cheaptripchip-api",
  "version": "1.0.0",
  "private": true,
  "description": "Vercel proxy (Tokyo region) that forwards cheaptripchip's Gemini calls, dodging the HK geo-block without any Flutter code change.",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22.10.5",
    "@vercel/node": "^15.0.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "type": "module"
}
